/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Mantem a tela limpa durante a apresentacao comercial.
  devIndicators: false,
};

export default nextConfig;
